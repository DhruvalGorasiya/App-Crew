// Navigation Key

import 'package:flutter/material.dart';

final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();

// Padding

const padding = EdgeInsets.only(top: 20, right: 20, left: 20);
const paddingH = EdgeInsets.symmetric(horizontal: 20);
const paddingV = EdgeInsets.symmetric(vertical: 20);
const paddingA = EdgeInsets.all(20);
